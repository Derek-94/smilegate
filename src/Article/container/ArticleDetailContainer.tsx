import React, { FunctionComponent } from 'react';

import ArticleDetail from '../presenter/ArticleDetail';

const ArticleDetailContainer: FunctionComponent = () => {
  return <ArticleDetail />;
};

export default ArticleDetailContainer;
